import 'package:flutter/material.dart';

class MainDrRent extends StatelessWidget {
  const MainDrRent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("메인(대여 중)"),
      ),
    );
  }
}
