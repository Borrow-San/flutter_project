import 'package:flutter/material.dart';

class UmbPhoto extends StatelessWidget {
  const UmbPhoto({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("우산 촬영 "),
      ),
    );
  }
}
